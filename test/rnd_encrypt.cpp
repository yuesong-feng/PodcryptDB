#include "RND.h"
#include "util.h"
#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main(){
    // RND rnd;
    // rnd.recover_keys("key/l2_key_det", "key/l2_key_ope");
    // string cipher_det = tohex(recover_str_hex("data/ciphertext_det"));
    // string cipher_ope = tohex(recover_str_hex("data/ciphertext_ope"));
    // cout << "cipher DET: " << cipher_det << endl;
    // cout << "cipher OPE: " << cipher_ope << endl;

    // vector<string> res = rnd.Encrypt(cipher_det, cipher_ope);

    // string cipher = res[0];
    // string l2_key_det = res[1];
    // string l2_key_ope = res[2];
    // cout << "cipher: " << cipher << endl;

    // store_str(cipher, "data/ciphertext_rnd");

    return 0;
}